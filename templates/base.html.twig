<!DOCTYPE html>
<html lang="es">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Café Latorta{% endblock %}</title>
        <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">

        <!--Bootstrap-->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
        <link rel="stylesheet" href="/assets/css/estilos.css">

        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body class="w-100 h-100vh fondo-oscuro">
        {% block header %}
            <header>
                <nav class="collapse fondo-vermello text-white" id="navbarHeader">
                    <div class="container">
                        <div class="row">
                            <div class="col pt-4">
                                <ul class="list-unstyled d-flex justify-content-center gap-4 flex-wrap">
                                    <li><strong><a href="#carta-bebidas" class="text-white text-decoration-none">Carta Cafetería</a></strong></li>
                                    <li><strong><a href="#carta-latorta" class="text-white text-decoration-none">Carta Latorta</a></strong></li>
                                    <!--Si a conta ca que está a sesión iniciada ten o rol ROLE_ADMIN mostramos enlaces as páxinas de xestión-->
                                    {% if app.user and 'ROLE_ADMIN' in app.user.roles %}
                                        <li><strong><a href="{{ path('app_produto_index') }}" class="text-white text-decoration-none">Xestión Produtos</a></strong></li>
                                        <li><strong><a href="{{ path('app_proveedor_index') }}" class="text-white text-decoration-none">Xestión Proveedores</a></strong></li>
                                    {% endif %}
                                    <!--Se a sesión está iniciada mostramos botón de pechar sesión, se non está iniciada mostramos botón de iniciar sesión-->
                                    <li><strong><a href="{{ app.user ? path('app_register') : path('app_login') }}" class="text-white text-decoration-none">{{ app.user ? "Pechar sesión" : "Iniciar sesión" }}{{ app.user ? "("~app.user.email~")" : "" }}</a></strong></li>
                                </ul>
                            </div>
                        </div>
                    </div>
                </nav>
                <div class="navbar navbar-dark fondo-vermello">
                    <div class="container">
                        <a href="/" class="navbar-brand d-flex align-items-center">
                            <strong class="text-white">Café Latorta</strong>
                        </a>
                        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarHeader" aria-controls="navbarHeader" aria-expanded="false" aria-label="Cambiar nav">
                            <span class="navbar-toggler-icon"></span>
                        </button>
                    </div>
                </div>
            </header>
        {% endblock %}

        <main class="fondo-oscuro min-h-100vh">
            {% block main %}{% endblock %}
        </main>
        
        {% block footer %}
            <footer class="text-white py-5 fondo-vermello">
                <div class="container">
                    <p class="float-end mb-1">
                        <a href="#" class="text-decoration-none text-white">Ir arriba</a>
                    </p>
                    <p class="mb-1">Todos los derechos reservados Café Latorta - &copy; 2023</p>
                </div>
            </footer>
        {% endblock %}
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ENjdO4Dr2bkBIFxQpeoTz1HIcje39Wm4jDKdf19U8gI4ddQ3GYNS7NTKfAdVQSZe" crossorigin="anonymous"></script>
    </body>
</html>